name: testing_exp_1
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core - Required to initialize Firebase
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.7
  google_sign_in: ^6.2.1
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  # You also need the flutter packages for hive to work smoothly



  # Your existing dependencies
  syncfusion_flutter_charts: ^29.1.35
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Uncomment and list your assets here if you add any
  # assets:
  #   - images/
